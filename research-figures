#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [[ "${1:-}" == "demo" ]]; then
  shift
  exec python3 "$ROOT_DIR/tools/demo_showcase.py" "$@"
fi

exec python3 "$ROOT_DIR/skills/research-figure-studio/scripts/run_figure_pipeline.py" "$@"
