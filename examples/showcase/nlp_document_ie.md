# Document-Level Information Extraction Overview

Generate a paper-style method overview illustration for an NLP system.

Core story:

- input a long document with multiple entities and relations
- encode the document with a hierarchical text encoder
- aggregate sentence-level and document-level context
- perform entity typing and relation extraction jointly
- output a structured knowledge graph

Visual elements:

- document pages on the left
- a central encoder and context aggregation module
- highlighted entity spans and relation cues
- a clean graph-style structured output on the right

Style:

- white background
- publication-quality method figure
- very limited readable labels
- emphasize information flow rather than UI
